/*import { createTheme } from '@mui/material/styles';

// Vytvoření vlastního tématu
const theme = createTheme({
  components: {
    MuiTooltip: {
      styleOverrides: {
        tooltip: {
          fontSize: '1.2rem',      // Zvětšení fontu
          padding: '8px 12px',     // Zvětšení paddingu
          lineHeight: '1.6',       // Zlepšení čitelnosti
          maxWidth: '350px',       // Zvětšení šířky
        },
      },
    },
  },
});

export default theme;*/